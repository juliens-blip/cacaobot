# ════════════════════════════════════════════════════════════════════════════
# 🌴 Palm Oil Trading Bot - Environment Configuration
# ════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env
#
# NEVER commit .env to version control!
#
# ════════════════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────────────────
# 📡 cTrader Open API Configuration
# ────────────────────────────────────────────────────────────────────────────
# Get your credentials from: https://help.ctrader.com/open-api/
#
# ⚠️  IMPORTANT: Start with DEMO credentials during development!
# See docs/OAUTH_PRODUCTION.md for migration guide.
# ────────────────────────────────────────────────────────────────────────────

# Environment: demo | live
# Controls which server and credentials to use
CTRADER_ENVIRONMENT=demo

# ────────────────────────────────────────────────────────────────────────────
# 🟢 DEMO CREDENTIALS (Development & Testing)
# ────────────────────────────────────────────────────────────────────────────

# Demo OAuth Client ID (from cTrader Open API application)
CTRADER_CLIENT_ID=your_demo_client_id_here

# Demo OAuth Client Secret (from cTrader Open API application)
CTRADER_CLIENT_SECRET=your_demo_client_secret_here

# Demo Trading Account ID (from cTrader demo account)
CTRADER_ACCOUNT_ID=your_demo_account_id_here

# OAuth Access Token (required for DEMO and LIVE)
# ⚠️ IMPORTANT: cTrader requires a valid OAuth token even for DEMO accounts
# Run this command to obtain your token:
#   cargo run --bin get-token
# Then paste the token below:
CTRADER_ACCESS_TOKEN=your_access_token_here

# Demo Server hostname (auto-configured based on CTRADER_ENVIRONMENT)
CTRADER_SERVER=demo.ctraderapi.com

# Server TCP port (5035 for both demo and live)
CTRADER_PORT=5035

# ────────────────────────────────────────────────────────────────────────────
# 🔴 LIVE PRODUCTION CREDENTIALS (Real Money Trading)
# ────────────────────────────────────────────────────────────────────────────
# ⚠️  WARNING: These credentials control REAL MONEY!
#
# NEVER commit live credentials to version control.
# Use Railway secrets or secure environment management in production.
#
# To enable LIVE trading:
# 1. Set CTRADER_ENVIRONMENT=live
# 2. Uncomment and fill the LIVE credentials below
# 3. Ensure DRY_RUN=false if you want real order execution
#
# See docs/OAUTH_PRODUCTION.md for full migration guide.
# ────────────────────────────────────────────────────────────────────────────

# CTRADER_CLIENT_ID_LIVE=your_live_client_id_here
# CTRADER_CLIENT_SECRET_LIVE=your_live_client_secret_here
# CTRADER_ACCOUNT_ID_LIVE=your_live_account_id_here

# ────────────────────────────────────────────────────────────────────────────
# 🧠 Perplexity API Configuration
# ────────────────────────────────────────────────────────────────────────────
# Get your API key from: https://www.perplexity.ai/settings/api
# ────────────────────────────────────────────────────────────────────────────

# Perplexity API Key (starts with pplx-)
PERPLEXITY_API_KEY=pplx-YOUR_API_KEY_HERE

# Perplexity API endpoint (default: https://api.perplexity.ai/chat/completions)
PERPLEXITY_ENDPOINT=https://api.perplexity.ai/chat/completions

# Model to use (sonar = real-time web search, sonar-pro = advanced)
PERPLEXITY_MODEL=sonar

# ────────────────────────────────────────────────────────────────────────────
# 📊 Trading Configuration
# ────────────────────────────────────────────────────────────────────────────
# Core trading parameters
# ────────────────────────────────────────────────────────────────────────────

# Trading symbol (FCPO = Crude Palm Oil Futures)
SYMBOL=FCPO

# Risk percentage per trade (1.0 = 1% of account balance)
# Example: On $10,000 account, risk $100 per trade
RISK_PER_TRADE=1.0

# Take Profit threshold in percentage (2.0 = close position at +2% profit)
TAKE_PROFIT_PERCENT=2.0

# Stop Loss threshold in percentage (1.5 = close position at -1.5% loss)
STOP_LOSS_PERCENT=1.5

# Maximum number of concurrent open positions (1 = one at a time)
MAX_POSITIONS=1

# Maximum daily loss percentage before circuit breaker triggers (5.0 = -5%)
# Trading stops for the day when this threshold is hit
MAX_DAILY_LOSS_PERCENT=5.0

# ────────────────────────────────────────────────────────────────────────────
# 🎯 Strategy Parameters
# ────────────────────────────────────────────────────────────────────────────
# RSI and sentiment analysis configuration
# ────────────────────────────────────────────────────────────────────────────

# RSI calculation period (14 = 14 candles, standard)
RSI_PERIOD=14

# RSI oversold threshold (30 = oversold when RSI < 30)
RSI_OVERSOLD=30

# RSI overbought threshold (70 = overbought when RSI > 70)
RSI_OVERBOUGHT=70

# Timeframe for RSI calculation (5m, 15m, 1h, etc.)
# Note: 5m = 5-minute candles for scalping
RSI_TIMEFRAME=5m

# Sentiment score threshold (30 = need sentiment > +30 for buy, < -30 for sell)
# Range: -100 (very bearish) to +100 (very bullish)
SENTIMENT_THRESHOLD=30

# ────────────────────────────────────────────────────────────────────────────
# 🐦 Twitter KOLs (Backup Sentiment Source)
# ────────────────────────────────────────────────────────────────────────────
# Key Opinion Leaders to monitor when Perplexity API is unavailable
# ────────────────────────────────────────────────────────────────────────────

KOL_1=PalmOilTrader
KOL_2=CommodityInsights
KOL_3=AgriMarketsDaily
KOL_4=OilseedMarkets
KOL_5=SEAsiaTrader

# ────────────────────────────────────────────────────────────────────────────
# 🤖 Bot Runtime Settings
# ────────────────────────────────────────────────────────────────────────────
# Bot behavior and execution parameters
# ────────────────────────────────────────────────────────────────────────────

# Main loop cycle interval in seconds (60 = check market every 60 seconds)
CYCLE_INTERVAL_SECS=60

# Dry run mode (true = simulation only, no real orders; false = live trading)
# ⚠️ ALWAYS use true for testing!
DRY_RUN=true

# ────────────────────────────────────────────────────────────────────────────
# 📦 Persistence (SQLite)
# ────────────────────────────────────────────────────────────────────────────
# SQLite file path for trade/position persistence
# Default: data/positions.db
PERSISTENCE_DB_PATH=data/positions.db

# ────────────────────────────────────────────────────────────────────────────
# 📈 Prometheus Metrics Export
# ────────────────────────────────────────────────────────────────────────────
# Enable /metrics endpoint for scraping (Prometheus)
METRICS_ENABLED=false
# Bind host for metrics server (default localhost only)
METRICS_HOST=127.0.0.1
# Bind port for metrics server
METRICS_PORT=9090

# ────────────────────────────────────────────────────────────────────────────
# 📝 Logging Configuration
# ────────────────────────────────────────────────────────────────────────────
# Log level for the bot (debug, info, warn, error)
# ────────────────────────────────────────────────────────────────────────────

# Log level (debug = verbose, info = normal, warn = warnings only, error = errors only)
# Recommended: info for production, debug for development
RUST_LOG=info

# ════════════════════════════════════════════════════════════════════════════
# End of Configuration
# ════════════════════════════════════════════════════════════════════════════
#
# ⚠️  IMPORTANT REMINDERS:
#
# 1. NEVER commit .env to Git (it's in .gitignore)
# 2. ALWAYS start with DRY_RUN=true
# 3. Test THOROUGHLY on demo account before going live
# 4. Keep your API keys secure
# 5. Monitor the bot actively, especially in the first days
#
# 📚 Documentation: See README.md for detailed setup instructions
# 🐛 Issues: https://github.com/yourusername/palm-oil-bot/issues
#
# ════════════════════════════════════════════════════════════════════════════
