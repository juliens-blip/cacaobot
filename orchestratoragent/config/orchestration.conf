#!/bin/bash
# =============================================================================
# Orchestration Configuration
# Multi-LLM Orchestration System for Palm Oil Bot
# =============================================================================

# Session name for tmux
SESSION_NAME="palm-oil-orchestration"

# Paths
PROJECT_DIR="/home/julien/Documents/palm-oil-bot"
CLAUDE_MD_PATH="$PROJECT_DIR/CLAUDE.md"
AGENTS_DIR="$PROJECT_DIR/.claude/agents"
ORCHESTRATOR_DIR="$PROJECT_DIR/orchestratoragent"
LOG_DIR="$ORCHESTRATOR_DIR/logs"
PROMPTS_DIR="$ORCHESTRATOR_DIR/prompts"

# LLM Commands
CLAUDE_CMD="claude --dangerously-skip-permissions"
AMP_CMD="amp -m large --dangerously-allow-all"
CODEX_CMD="codex --dangerously-bypass-approvals-and-sandbox"
ANTIGRAVITY_PROXY_CMD="antigravity-claude-proxy start"

# Antigravity Client Configuration
ANTIGRAVITY_PROXY_URL="http://localhost:8080"
ANTIGRAVITY_AUTH_TOKEN="test"
ANTIGRAVITY_MODEL="claude-opus-4-5-thinking"

# Timeouts (seconds) - INCREASED for LLM startup
LLM_STARTUP_WAIT=10
PROXY_STARTUP_WAIT=8
ANTIGRAVITY_CLIENT_WAIT=12
PROMPT_DELAY=3

# Logging
LOG_LEVEL="INFO"  # DEBUG, INFO, WARN, ERROR

# Colors for terminal output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
MAGENTA='\033[0;35m'
NC='\033[0m' # No Color

# Orchestrator (can be: claude, amp)
CURRENT_ORCHESTRATOR="claude"
